workflows:
  Coordinator-workflow:
    name: Coordintor pipeline
    scripts: 
        - name: iOS test
          script: |
          xcode-project run-tests \
              --workspace Coordinator.xcworkspace \
              --scheme Coordinator \
    publishing:
      email:
        recipients:
        - manish123kumar79@gmail.com